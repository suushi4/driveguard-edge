services:
  - type: web
    name: driveguard-monitor
    runtime: static
    buildCommand: echo "DriveGuard static site ready"
    staticPublishPath: ./public
    autoDeploy: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
