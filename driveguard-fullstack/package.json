{
  "name": "driveguard-fullstack",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "Zero-dependency full-stack driver safety dashboard",
  "scripts": {
    "build": "node --check server.mjs && node --check public/app.js",
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
