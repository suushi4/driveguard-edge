import{useCallback,useState}from'react';
export function useDevice(){const[url,setUrlState]=useState(()=>localStorage.getItem('dg-device')||'http://192.168.1.100'),[online,setOnline]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');const setUrl=(v:string)=>{setUrlState(v);localStorage.setItem('dg-device',v)};const command=useCallback(async(path:string)=>{setBusy(true);try{const target=url.replace(/\/$/,'')+path,r=await fetch('/api/device?url='+encodeURIComponent(target),{signal:AbortSignal.timeout(3500)});if(!r.ok)throw Error('Устройство не отвечает');setOnline(true);setError('');return true}catch(e){setOnline(false);setError(e instanceof Error?e.message:'Ошибка сети');return false}finally{setBusy(false)}},[url]);return{url,setUrl,online,busy,error,command,stream:url.replace(/\/$/,'')+':81/stream'}}
