import{Icon}from'./Icons';export function DevicePanel({device,onAlarm,onStop}:{device:any;onAlarm:()=>void;onStop:()=>void}){return <section className="device-card"><div className="section-head"><div><span>ESP32-CAM</span><h2>Оборудование</h2></div><i className={device.online?'online':'offline'}>{device.online?'ONLINE':'OFFLINE'}</i></div><label>Адрес устройства<div className="url-field"><input value={device.url} onChange={e=>device.setUrl(e.target.value)} spellCheck={false}/><button onClick={()=>device.command('/api/status')} disabled={device.busy}>Проверить</button></div></label>{device.error&&<small className="error">{device.error}</small>}<div className="device-actions"><button className="alarm" onClick={onAlarm}><Icon name="bell"/>Тревога</button><button onClick={onStop}><Icon name="stop"/>Отключить</button></div><div className="hint"><Icon name="wifi"/><span>Для Render используйте публичный HTTPS-адрес защищённого локального моста.</span></div></section>}
