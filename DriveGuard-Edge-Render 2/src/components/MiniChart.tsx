import{useEffect,useRef}from'react';import type{Sample}from'../types';export function MiniChart({data}:{data:Sample[]}){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current,x=c?.getContext('2d');if(!c||!x)return;const d=devicePixelRatio,w=c.clientWidth,h=c.clientHeight;c.width=w*d;c.height=h*d;x.setTransform(d,0,0,d,0,0);x.clearRect(0,0,w,h);x.strokeStyle='rgba(255,255,255,.07)';for(let i=1;i<4;i++){x.beginPath();x.moveTo(0,h*i/4);x.lineTo(w,h*i/4);x.stroke()}const line=(key:'score'|'ear',color:string,min:number,max:number)=>{x.beginPath();data.forEach((v,i)=>{const px=i/(data.length-1)*w,py=h-(v[key]-min)/(max-min)*h;i?x.lineTo(px,py):x.moveTo(px,py)});x.strokeStyle=color;x.lineWidth=2;x.stroke()};line('score','#b7ff5a',0,100);line('ear','#57d8ff',0,.4)},[data]);return <section className="chart-card"><div className="section-head"><div><span>LAST 60 SECONDS</span><h2>Телеметрия</h2></div><div className="legend"><i/>Внимательность <i/>EAR</div></div><canvas ref={ref}/></section>}
