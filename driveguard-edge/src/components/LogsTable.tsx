import React,{useState} from 'react';const rows=[['14:32:18','Microsleep','2.4s','Critical'],['14:18:42','Yawn','4.1s','Medium'],['13:57:09','Distraction','6.8s','Low'],['13:41:26','Microsleep','1.8s','Critical']];export function LogsTable({t}:{t:any}){const [filter,setFilter]=useState('All');const shown=rows.filter(r=>filter==='All'||r[3]===filter);const csv=()=>{const b=new Blob([['Time,Event,Duration,Severity',...rows.map(r=>r.join(','))].join('\n')],{type:'text/csv'});const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='driveguard-incidents.csv';a.click()};return <section className="panel logs"><div className="panelHead"><div><span className="eyebrow">SAFETY RECORD</span><h2>{t.logs}</h2></div><div className="tableActions"><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="All">{t.all}</option><option>Critical</option><option>Medium</option><option>Low</option></select><button className="btn ghost" onClick={csv}>⇩ {t.export}</button></div></div><div className="tableWrap"><table><thead><tr><th>TIME</th><th>EVENT</th><th>DURATION</th><th>SEVERITY</th><th></th></tr></thead><tbody>{shown.map((r,i)=><tr key={i}><td><code>{r[0]}</code></td><td>{r[1]}</td><td>{r[2]}</td><td><span className={'severity '+r[3].toLowerCase()}>{r[3]}</span></td><td><button className="snap">▣ {t.snapshot}</button></td></tr>)}</tbody></table></div></section>}